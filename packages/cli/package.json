{
  "name": "@zeitgeistpm/cli",
  "version": "1.0.0",
  "description": "Zeitgeist network command line tools.",
  "main": "../../dist/cli/index.js",
  "types": "../../dist/cli/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "rebuild": "rm -rf dist; yarn dist",
    "start": "ts-node src/index start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@zeitgeistpm/sdk": "^1.0.0",
    "@zeitgeistpm/type-definitions": "^1.0.9",
    "commander": "^6.1.0"
  },
  "devDependencies": {
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
